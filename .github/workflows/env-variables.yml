name: Environment Variables
on:
  push:
    branches:
      - master
env:
  CHANNEL_NAME: CodeGeeks
jobs:
  envdemo:
    runs-on: ubuntu-latest
    steps:
      - name: EnvDemo
        env:
         CHANNEL_NAME: CodeGeeks
        run: echo "Hello from ${{env.CHANNEL_NAME}}"
        
      - name: Get Comments on ${{env.CHANNEL_NAME}}"
        run: echo "Awesome" echo "Geat Content" echo "Thank you for sharing"
          
  envdemo2:
    runs-on: ubuntu-latest
    steps:
      - name: EnvDemo2
        run: echo "Hello from ${{env.CHANNEL_NAME}} from envdemo2"

