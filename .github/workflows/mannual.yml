name: Mannual Worlkflow

on:
  workflow_dispatch:
    inputs:
      name:
       description: 'Name'
       type: string
       default: 'Anil'
jobs:
  manual:
   runs-on: ubuntu-latest
   
   steps:
     - name: MyMannual
       run: "${{inputs.name}}"
       
